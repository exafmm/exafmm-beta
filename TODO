-- CPU/GPU --
.h -> .cxx/.cu
unify dataset
Partitioning with Hilbert
Kahan + fixed precision
autotools + regression -> ExaFMM 1.0 release
Solid harmonics kernel

-- CPU --
1. Remove macro for Cartesian
2. Detatch RMAX and RCRIT calculation
3. Remove RMAX and RCRIT from Cell struct
bodies -> bodies + fields, bodyPos -> bodies, bodyAcc -> fields
SIMD P2P performance

-- GPU --
MPI Bonsai
Ewald, VdW
Zero softening

-- Comparisons --
DTT vs. UVWX-list
Cartesian vs. Spherical (optimized)