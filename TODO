-- CPU/GPU --
.h -> .cxx/.cu
unify dataset
Kahan + fixed precision
autotools + regression -> ExaFMM 1.0 release
Solid harmonics kernel

-- CPU --
1. Detatch RMAX and RCRIT calculation
2. Remove RMAX and RCRIT from Cell struct
3. Partitioning with Hilbert
4. Merge LET before dualTreeTraversal
5. Fix parallel ewald
6. setLET multithread
bodies -> bodies + fields, bodyPos -> bodies, bodyAcc -> fields
SIMD P2P performance

-- GPU --
MPI Bonsai
Ewald, VdW
Zero softening

-- Comparisons --
DTT vs. UVWX-list
Cartesian vs. Spherical (optimized)